venv/
chroma_db/
__pycache__/
*.pyc
.DS_Store

# Personal PYQ collection — keep folder structure, not the actual papers
data/pdfs/*/

# Built extension package — rebuild with mcpb pack, don't commit the binary
*.mcpb

# Pyrefly config
pyrefly.toml