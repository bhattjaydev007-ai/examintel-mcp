[project]
name = "examintel-mcp"
version = "1.0.0"
description = "MCP server exposing semantic search and study-plan generation over your own exam question papers."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.27.0",
    "chromadb>=1.5.0",
    "pdfplumber>=0.11.0",
    "fastembed>=0.8.0",
]
